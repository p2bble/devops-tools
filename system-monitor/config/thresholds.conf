# 임계값 설정

# CPU 사용률 (%)
CPU_WARNING=70
CPU_CRITICAL=85

# 메모리 사용률 (%)
MEMORY_WARNING=75
MEMORY_CRITICAL=90

# SWAP 사용률 (%)
SWAP_WARNING=50
SWAP_CRITICAL=80

# 디스크 사용률 (%)
DISK_WARNING=85
DISK_CRITICAL=95

# 로드 애버리지
LOAD_WARNING=3.0
LOAD_CRITICAL=8.0

# 네트워크 패킷 드롭률 (%)
NETWORK_DROP_WARNING=1.0
NETWORK_DROP_CRITICAL=5.0

# VM별 메모리 사용률 (%)
VM_MEMORY_WARNING=80
VM_MEMORY_CRITICAL=90
